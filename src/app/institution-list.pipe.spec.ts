import { InstitutionListPipe } from './institution-list.pipe';

describe('InstitutionListPipe', () => {
  it('create an instance', () => {
    const pipe = new InstitutionListPipe();
    expect(pipe).toBeTruthy();
  });
});
