import { InstitutionsPipe } from './institutions.pipe';

describe('InstitutionsPipe', () => {
  it('create an instance', () => {
    const pipe = new InstitutionsPipe();
    expect(pipe).toBeTruthy();
  });
});
